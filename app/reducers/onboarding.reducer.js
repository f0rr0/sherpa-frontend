import { INIT_ONBOARDING } from '../actions/onboarding.actions';

export default function onBoardingReducer(state = 0, action) {
    switch (action.type) {
        default:
            return state;
    }
}