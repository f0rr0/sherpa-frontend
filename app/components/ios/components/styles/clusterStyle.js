import {
    StyleSheet,
} from 'react-native';

export default StyleSheet.create({
    clusterContainer: {
        backgroundColor: '#666',
        height: 30,
        width: 30,
        borderRadius: 15,
        justifyContent: 'center',
        alignItems: 'center',
        borderColor: '#fff',
        borderWidth: StyleSheet.hairlineWidth,
    },
    clusterText: {
        color: '#fff',
        fontSize: 12,
    },
});